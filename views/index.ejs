<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini WhatsApp</title>

    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

</head>
<body>
    <h1 style="font-size: 5rem;">Blog Website</h1>
    <br><br>
    <!-- chats/new route -->
    <!-- action me pura isliye url n likha as jo pehle se url h use me ye parameter add ho jayege -->
    <div class="" style="width:75%; margin:auto;">
        
    <form method="GET" action="/chats/new">
        <button class="btn btn-success btn-md" style="text-align: center ; justify-content: center;">New Chat</button>
    </form>
    </div>
    <br>
    
    <% for(chat of chats){ %>
        <div class="chat" style="width:75%; margin:auto;">
        
            <p>Created By : <i style="color:crimson; font-size:2rem;"><%=chat.from%></i></p>
            <div class="msg">
                
                <p><%=chat.msg%></p>
                <div class="par-div">
                    <form method="GET" action="/chats/<%=chat._id%>">
                        <button class="btn btn-light" style="color:Black;">Show</button>
                    </form>
                    <form method="GET" action="/chats/<%=chat._id%>/edit">
                        <button class="btn btn-warning">Edit</button>
                    </form>
                    <form method="POST" action="/chats/<%=chat._id%>?_method=DELETE">
                        <button class="btn btn-danger">Delete</button>
                    </form>
                    
                    
                    
                </div>
                
            </div>
            <!-- <p>Received by : <i><%=chat.to%></i></p> -->
            <hr>
            <!-- <p><%=chat.created_at.toString().split(" ")[4]%></p> -->
            <p><%=chat.created_at.toString().split(" ").slice(0,4).join("-")%></p>
        </div>
        <br><br>
    <% } %>
</body>
</html>

